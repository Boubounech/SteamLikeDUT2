{% extends "base.html.twig" %}

{% block title %}Rechercher Un Jeu{% endblock %}

{%  block body %}
    <h1>Bibliothèque</h1>
    <p>Rechercher</p>
    <form action="{{ path("SearchGames") }}" method="get">
        <label for="tit">Titre : </label>
        <input type="text" name="tit" id="tit">

        <label for="cate">Catégorie : </label>
        <select name="cate" id="cate">
            <option value="">--Choisir une Catégorie--</option>
            <option value="R-18 Games">R-18 Games</option>
            <option value="Familial">Familial</option>
            <option value="Science-Fiction">Science-Fiction</option>
            <option value="Historique">Historique</option>
        </select>
        <button type="submit"> Rechercher </button>
    </form>
    <p>Vous avez cherché : </p>
    <p>Nom = {{ title }}</p>
    <p>Catégorie = {{ category }}</p>
    <p>Voilà les résulats de la recherche : </p>
    {% for game in games %}
        <h3>{{ game.title }}</h3>
        <a href="{{ path("game", {id:game.id}) }}">Voir plus.</a>
    {% endfor %}
    <a href="{{ path("index") }}">Revenir à l'accueil</a>
{% endblock %}