{% extends "base.html.twig" %}

{% block title %}Changer les Informations du Jeu{% endblock %}

{%  block body %}
    <h1>Changer les données de {{ game.title }}</h1>
    {{ form_start(form) }}
    <div class="FormUser">
        <div class="FormPart">{{ form_row(form.title) }}</div>
        <div class="FormPart">{{ form_row(form.category) }}</div>
        <div class="FormPart">{{ form_row(form.link) }}</div>
        <div class="FormPart">{{ form_row(form.description) }}</div>
    <button type="submit" class="SubmitButton">Changer les informations</button>
    </div>
    {{ form_end(form) }}
    <a class="DeleteButton" href="{{ path("DeleteGame", {id:game.id}) }}">Supprimer le jeu</a>
{% endblock %}